import React, { useState } from 'react';
import api from '../api/axios'; // Importante: ajusta la ruta a tu archivo axios

const ProfileUpload = () => {
  // ... (aquí va todo el código que pusiste)
};

export default ProfileUpload;