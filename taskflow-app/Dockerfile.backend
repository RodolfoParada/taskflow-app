FROM node:18-alpine
WORKDIR /app

# Como el contexto es la carpeta nivel 2, 
# solo tenemos que entrar a 'backend/'
COPY backend/package*.json ./
RUN npm install
COPY backend/ .

EXPOSE 3001
CMD ["npm", "run", "dev"]