FROM node:18-alpine
WORKDIR /app

# 1. Ya estamos dentro de la carpeta nivel 2, 
# así que buscamos directamente 'backend'
COPY backend/package*.json ./

RUN npm install

# 2. Copiamos el contenido de la subcarpeta backend al WORKDIR
COPY backend/ .

EXPOSE 3001
CMD ["npm", "run", "dev"]